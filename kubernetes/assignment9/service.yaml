apiVersion: v1
kind: Service
metadata:
  name: devopsapp
  namespace: team-payments
  labels:
    app: devopsapp
spec:
  selector:
    app: devopsapp
  ports:
    - name: http
      port: 80
      targetPort: 8080
