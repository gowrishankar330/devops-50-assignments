apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: devopsapp
  namespace: team-paymentshelm repo update
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: devopsapp
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
